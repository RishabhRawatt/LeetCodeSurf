class Solution {
public:
    int bulbSwitch(int n) {
        
        //intution 
        // only perfect square nos will be remain on (because they have odd no of root pairs so if the total root pairs are odd then means it will left to on condition )
        
        
       return sqrt(n);
    }
};